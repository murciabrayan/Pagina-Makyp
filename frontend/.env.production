# Se usa SOLO al compilar para producción (npm run build).
# `.env` sigue mandando en desarrollo, así que no hace falta cambiar nada
# para volver a trabajar en local.
#
# Sustituye esta dirección por la del backend ya desplegado, sin barra final.
VITE_API_URL=https://CAMBIA-ESTO.northflank.app/api
